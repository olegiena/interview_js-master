
import React from 'react';
import { Typography } from 'antd';
const { Title } = Typography;

const NotFound = () => (
  <Title level={3}>Sorry but we can't seem to find the page you are looking for</Title>
);

export default NotFound;
